# Create a hash with: npm run hash-password -- "your password"
# Escape every $ as \$ when pasting the result here because Next expands $VARS.
YOSYA_PASSWORD_HASH=

# Generate a long random value, for example: openssl rand -hex 32
SESSION_SECRET=
