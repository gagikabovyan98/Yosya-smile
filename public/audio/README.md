# Audio placeholders

`AudioManager` already supports these optional files when original or royalty-free assets are supplied:

- `ambient-room.mp3`
- `artwork-tear.mp3`

The current vertical slice uses small procedural Web Audio cues for the church flash, paper movement, candle extinguish and final fragment. Missing optional files fail silently.

